import {register, onNewMessage, onNewInteraction, setup} from './message';

export default {
  setup: setup,
  register: register,
  onNewMessage: onNewMessage,
  onNewInteraction: onNewInteraction,
};
